# Specification workbook

Transcribe the source PDF into atomic, approved rules before implementation. Each rule requires: stable ID, preconditions, inputs and units, exact formula/transition, rounding, failure behavior, ReasonCode, source page, and approval status.

No rule may be implemented until its exact values are approved and it has a Rule ID. Do not guess from corrupted or ambiguous PDF text.
